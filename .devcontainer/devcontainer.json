{
  "name": "SDL3 Headless Codespace",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "postStartCommand": "mkdir -p build && cd build && cmake .. && cmake --build .",
  "customizations": {
    "vscode": {
      "extensions": []
    }
  }
}